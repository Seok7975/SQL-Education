다음 관계형 모델 용어를 그에 대응하는 SQL 용어로 써보자.(1~3)

1. 릴레이션 - 
2. 속성 - 
3. 튜플 - 
 
4. A 테이블을 만들고 다음 요소를 가지도록 만든다.
no int

5. 그리고 A테이블에 순차적으로 1, 2, 3을 넣는다.
6. B 테이블을 만들고 다음 요소를 가지도록 만든다.
no int

7. 그리고 B테이블에 순차적으로 2, 10, 11을 넣는다.

8. A 테이블과 B 테이블의 합집합을 구한다.

9. A 테이블과 B 테이블의 차집합을 구한다.

10. A 테이블과 B 테이블의 교집합을 구한다.

11. A 테이블과 B 테이브 교집합을 구한다.

연습문제

- 문제1
테이블을 결합할 때 결합조건을 지정하지 않고 실행하면 구할 수 있는 집합은 무엇인가?
① 합집합
② 교집합
③ 곱집합

- 문제2
내부결합 할 때 사용하는 키워드는 무엇인가?
① INNER JOIN
② LEFT JOIN
③ RIGHT JOIN

- 문제3
관계형 모델에서 릴레이션에 해당하는 SQL 용어는 무엇인가?
① 테이블
② 열
③ 행