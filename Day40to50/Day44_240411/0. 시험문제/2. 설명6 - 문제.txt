* 다음으로 sample37 테이블을 만든다.
a 정수

* 다음 3개의 데이터를 sample37에 삽입한다.
1
2
NULL

* sample37 테이블에서 a를 출력하고 "a(null=0)"이라는 별명으로 
  a의 값을 출력하는 데 NULL은 0으로 출력하자.

* sample37 테이블에서 a를 출력하고 "a의값중 null을 찾는용도"라는 별명으로 
  a의 값을 출력하는 데 NULL은 0으로 출력하자.

* 문자화하는 것을 (         )라 부르고 반대로 수치화하는 것을 (         )라고 부른다.

코드=수치 데이터			정보
	1	    ---(         )--->	남
	2	   <---(         )---	여

* sample37 테이블에서 a열을 별명 "코드"로 출력하고, 1은 '남자' 2는 '여자' 
그 밖에는 '미지정'으로 출력하는 데 열명은 "성별"이라고 하자.
(검색 case이용)
SELECT a AS "코드",
CASE
	WHEN a = 1 THEN '남자'
	WHEN a = 2 THEN '여자'
	ELSE '미지정'
END 
AS "성별" FROM sample37;

* sample37 테이블에서 a열을 별명 "코드"로 출력하고, 
1은 '남자' 2는 '여자' 그 밖에는 '미지정'으로 출력하는 데 열명은 "성별"이라고 하자.(단순 CASE 이용)
SELECT a AS "코드",
CASE a
	WHEN 1 THEN '남자'
	WHEN 2 THEN '여자'
	ELSE '미지정'
END 

* ELSE를 생략하면 ELSE ( NULL )이 되는 것에 주의하자. 상정한 것 이외의 데이터가 들어오는 경우도 많다. 대응하는 WHEN이 하나도 없으면 ELSE 절이 사용된다. 이때 ELSE를 생략하면 상정한 것 이외의 데이터가 왔을 때 (         )이 반환된다. 따라서 ELSE를 생략하지 않고 지정하는 편이 낫다.
CASE 문의 ELSE는 생략하지 않는 편이 낫다!

* NULL 값인지 아닌지를 판정하기 위해서는 ( IS NULL )을 사용한다. 다만 단순 CASE 문은 특성상 = 연산자로 비교하는 만큼, NULL 값인지를 판정하려면 검색 CASE 문을 사용해야 한다.

* a열을 별명 "코드"로 출력하고, 1은 '남자' 2는 '여자', NULL이면 '데이터 없음', 그 밖에는 '미지정'으로 출력하는 데 열명은 "성별"이라고 하자.

연습문제
- 문제1
ORDER BY 구에 의해 정렬되는 것은 무언인가?

① 행
② 열
③ 오름차순

-문제2
식 중에 NULL 값이 포함된 경우 연산결과는 무엇인가?

① 에러가 발생한다.
② 0
③ NULL

-문제3
SELECT 구로 식에 붙일 수 있는 것은 무엇인가?

① 별명
② CASE 문
③ ORDER BY